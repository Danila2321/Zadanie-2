s = input().strip()
t = input().strip()

if len(s) != len(t):
    print(-1)
else:
    doubled_s = s + s
    if t in doubled_s:
        pos = doubled_s.index(t)
        shift = (len(s) - pos) % len(s)
        print(shift)
    else:
        print(-1)
